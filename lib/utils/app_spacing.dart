/// Единообразные отступы между заголовками секций и контентом (карточками).
class AppSpacing {
  AppSpacing._();

  /// Расстояние между заголовком секции и карточками/контентом ниже.
  static const double headingToContent = 6;

  /// Расстояние между секциями (например, между баннером и блоком брендов).
  static const double sectionGap = 16;
}
